{
  "dependencies": {
    "bun-types": "^0.2.2",
    "zod": "^3.19.1"
  },
  "scripts": {
    "reset": "rm -rf db.sqlite db.sqlite-journal && bun ./src/server.ts",
    "start": "bun ./src/server.ts"
  },
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}
